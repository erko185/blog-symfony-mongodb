doctrine_mongodb:
    connections:
        default:
            server: "%mongodb_server%"
            options: {}
    default_database: blog
    document_managers:
        default:
            auto_mapping: true
